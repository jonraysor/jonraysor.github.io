/**
 * Entry point for about page
 */

import { redirectToConsulting, redirectToHomepage } from './navigation';

// Export for use in window scope
export { redirectToConsulting, redirectToHomepage };
