/**
 * Entry point for consulting page
 */

import { redirectToHomepage, redirectToAbout } from './navigation';
import { validateAndSubmit } from './formValidation';

// Export for use in window scope
export { redirectToHomepage, redirectToAbout, validateAndSubmit };
